# Developer Guide

## Installation
- Install XCode and Android Studio, configure as shown on the Tauri Install docs.
- Install mise for remaining dev toolchain configuration, as shown on the `mise` install docs and the `mise install`.